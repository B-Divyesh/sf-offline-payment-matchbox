[
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "landing facts, network status, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /demo/; wait for service worker; set context offline; reload sample ledger"
  },
  {
    "id": "demo-sample",
    "claim": "The sample workspace opens with three freelancer invoices and three downloaded payments",
    "where": "landing helper, demo lead, README",
    "test": "npm run test:e2e -- --grep @claim:demo-sample",
    "sandbox": "fresh /demo/ sample; assert both three-row trays and the named invoice and payment records shown by the sample"
  },
  {
    "id": "matching-signals",
    "claim": "Suggestions show amount, currency, reference, customer, and 45-day date evidence when each rule applies",
    "where": "sample suggestion reasons",
    "test": "npm run test:e2e -- --grep @claim:matching-signals",
    "sandbox": "fresh /demo/ sample; seed isolated demo-only fixtures and assert each visible suggestion reason, including the 45-day boundary"
  },
  {
    "id": "csv-report",
    "claim": "Exports matched invoices, open invoices, and unused payments as CSV",
    "where": "workspace, how it works, README",
    "test": "npm run test:e2e -- --grep @claim:csv-report",
    "sandbox": "fresh /demo/ sample; download report; assert header and one output row per sample record"
  },
  {
    "id": "csv-match",
    "claim": "Matches payments to invoices from two CSVs",
    "where": "landing, README",
    "test": "npm run test:e2e -- --grep @claim:csv-match",
    "sandbox": "fresh /?demo=1; replace the sample with shipped invoice and payment CSV fixtures; assert realistic suggestions and confirm one pair"
  },
  {
    "id": "private-workflow",
    "claim": "Parsing, matching, and report export keep reconciliation data on this device",
    "where": "landing, workspace, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:private-workflow",
    "sandbox": "fresh /demo/ sample; record every request through confirm and export; assert same-origin only"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo changes never enter the real workspace",
    "where": "demo banner, landing action, demo documentation",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "fresh /demo/ sample; change a match; choose Start for real; assert the real workspace is empty"
  },
  {
    "id": "local-persistence",
    "claim": "The active workspace persists in browser storage",
    "where": "privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:local-persistence",
    "sandbox": "fresh /demo/ sample; confirm a suggestion; reload; assert the decision remains"
  },
  {
    "id": "source-opt-in",
    "claim": "Raw source CSV text is stored only after the user opts in",
    "where": "mapping form, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:source-opt-in",
    "sandbox": "fresh /demo/ sample; import once without consent and once with consent; inspect downloaded backups"
  },
  {
    "id": "mapping-before-save",
    "claim": "Imported rows are saved only after column mapping is submitted",
    "where": "landing How it works step one",
    "test": "npm run test:e2e -- --grep @claim:mapping-before-save",
    "sandbox": "fresh /demo/ sample; open invoice and payment mapping forms; byte-compare demo IndexedDB before each submission; assert only submitted rows replace stored records"
  },
  {
    "id": "deterministic-review",
    "claim": "Deterministic suggestions flag competing matches and never silently merge them",
    "where": "README, terms",
    "test": "npm run test:e2e -- --grep @claim:deterministic-review",
    "sandbox": "fresh /demo/ sample; replace with two equal invoices and one payment; assert both need review and no quick confirm exists"
  },
  {
    "id": "json-backup",
    "claim": "Exports and imports all local records as a JSON backup",
    "where": "workspace, README",
    "test": "npm run test:e2e -- --grep @claim:json-backup",
    "sandbox": "fresh /demo/ sample; change a decision, export JSON, reset, import it, and assert every record returns"
  },
  {
    "id": "plus-batch",
    "claim": "Matchbox Plus costs $19 once and confirms all clear strong suggestions together",
    "where": "landing, Plus dialog, README, terms",
    "test": "npm run test:e2e -- --grep @claim:plus-batch",
    "sandbox": "fresh /demo/ sample with a locally cached valid fixture verdict; assert price; run batch confirm; assert every invoice is matched"
  },
  {
    "id": "free-core",
    "claim": "The free matcher includes manual review, reports, backups, and offline use",
    "where": "landing, Plus dialog, README, terms",
    "test": "npm run test:e2e -- --grep @claim:free-core",
    "sandbox": "fresh /?demo=1 without a license; save a manual match, export CSV and JSON, then reload the saved workspace offline"
  },
  {
    "id": "plus-column-mappings",
    "claim": "Matchbox Plus remembers repeat column mappings",
    "where": "landing, Plus dialog, README, terms",
    "test": "npm run test:e2e -- --grep @claim:plus-column-mappings",
    "sandbox": "fresh /demo/ sample with a locally cached valid fixture verdict; map unfamiliar invoice headings; reopen the same heading set; assert every saved selection is restored"
  },
  {
    "id": "manual-note",
    "claim": "Every manual match requires an audit note",
    "where": "workspace, README",
    "test": "npm run test:e2e -- --grep @claim:manual-note",
    "sandbox": "fresh /demo/ sample; select a manual payment; assert submission is blocked until a note is entered"
  },
  {
    "id": "daily-license-check",
    "claim": "License verification is cached for one day and its result stays visible",
    "where": "privacy page, license dialog",
    "test": "npm run test:e2e -- --grep @claim:daily-license-check",
    "sandbox": "fresh /demo/ sample with recorded invalid verification response; restore once; reload; assert no second request"
  },
  {
    "id": "billing-routing",
    "claim": "Checkout and license verification use the Sociobot billing API",
    "where": "Plus dialog, README",
    "test": "npm run test:e2e -- --grep @claim:billing-routing",
    "sandbox": "fresh /?demo=1; inspect the checkout link and record a fixture verification request"
  },
  {
    "id": "license-request-privacy",
    "claim": "License checks send only the license token to Sociobot",
    "where": "privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:license-request-privacy",
    "sandbox": "fresh /?demo=1 with ledger data; intercept a fixture verification and assert its URL, headers, and body contain no reconciliation data"
  },
  {
    "id": "workspace-clearing",
    "claim": "Clear local workspace removes this browser's workspace data",
    "where": "workspace, privacy page",
    "test": "npm run test:e2e -- --grep @claim:workspace-clearing",
    "sandbox": "fresh /demo/ sample; accept the specific clear confirmation; assert the workspace view is empty and the current record is absent from demo IndexedDB"
  },
  {
    "id": "tracking-free",
    "claim": "The product includes no advertising cookies, behavioral analytics, third-party fonts, or tracking scripts",
    "where": "privacy page",
    "test": "npm run test:e2e -- --grep @claim:tracking-free",
    "sandbox": "fresh /demo/ sample; exercise confirm and export; inventory cookies, network resource types, script URLs, and font loads"
  },
  {
    "id": "license-restore",
    "claim": "Buyers may restore a valid Matchbox Plus license on another device",
    "where": "Plus dialog, terms",
    "test": "npm run test:e2e -- --grep @claim:license-restore",
    "sandbox": "fresh /demo/ browser with a recorded valid verification fixture; paste a license; assert Plus is active, the token is stored, and a Plus action works"
  }
]
